# ==========================================
# Phase 3 Backend - Python FastAPI
# ==========================================

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart>=0.0.9

# Database
sqlalchemy==2.0.49
asyncpg==0.31.0
aiosqlite==0.20.0
alembic==1.13.1

# AI / Agent
langchain>=1.2.0,<2.0.0
langchain-openai>=1.1.12,<2.0.0
langgraph>=1.1.5,<1.2.0

# MCP (Model Context Protocol)
mcp>=1.0.0
langchain-mcp-adapters>=0.1.0

# Checkpoint 持久化（M5）
langgraph-checkpoint-sqlite>=3.0.0

# 生产用 Postgres（可选，水平扩展时装）：
#   业务库：asyncpg（已在上方 Database 区）+ Alembic 迁移（已含）
#   对话状态 Checkpointer 多机共享：
#     pip install "psycopg[binary]" langgraph-checkpoint-postgres
#   然后把 DATABASE_URL 换成 postgresql+asyncpg://...，并 `alembic upgrade head`

# 可观测（M6 - Langfuse）
langfuse>=2.0.0

# RAG（M9 - 向量检索）为可选功能，默认关闭（ENABLE_RAG=false）
# 重型依赖（chromadb + sentence-transformers ≈ 300MB）已移到 requirements-rag.txt
# 需要 RAG 时再装：pip install -r requirements-rag.txt

# Data Validation
pydantic>=2.7.4,<3.0.0
pydantic-settings>=2.10.1,<3.0.0

# Streaming
sse-starlette==1.8.2

# Utilities
python-dotenv==1.0.1
loguru==0.7.2
tenacity==8.2.3

# Testing
pytest>=8.2,<10
pytest-asyncio>=1.3.0,<2

httpx>=0.27.0               # 异步 HTTP 客户端（在线 Skill 搜索）
