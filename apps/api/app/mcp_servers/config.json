{
  "_comment": "MCP Server 注册表。增加/删除工具集只需改这里，不用动 agent 代码。",
  "mcpServers": {
    "weather": {
      "command": "python",
      "args": ["-m", "app.mcp_servers.weather_server"],
      "transport": "stdio",
      "_description": "天气查询（Open-Meteo API）· readOnly + openWorld"
    },
    "utils": {
      "command": "python",
      "args": ["-m", "app.mcp_servers.utils_server"],
      "transport": "stdio",
      "_description": "通用工具（计算器 + 搜索）· readOnly"
    },
    "dangerous": {
      "command": "python",
      "args": ["-m", "app.mcp_servers.dangerous_server"],
      "transport": "stdio",
      "_description": "危险工具（删除/转账）· M5 HITL 演示 · destructive"
    }
  }
}
